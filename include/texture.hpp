#ifndef __TEXTURE__
#define __TEXTURE__

#include "core.hpp"
#include <string>

// class Shader;

// class Texture {
// public:
// 	Texture(const std::string& path, unsigned int unit, const std::string& sampler2D,
// 			int min_filter = GL_LINEAR_MIPMAP_LINEAR, int mag_filter = GL_LINEAR,
// 			int wrap_s = GL_REPEAT, int wrap_t = GL_REPEAT);
// 	~Texture();

//     void bind();

//     operator int() {
//         return mTexture;
//     }

// private:
// 	unsigned int mTexture{ 0 };
// 	int mWidth{ 0 };
// 	int mHeight{ 0 };

//     // 激活的纹理单元
// 	unsigned int mUnit{ 0 };
// };

#endif